// Correctnes: ok
// /Users/mstanchin/share/git-reps/binaryninja/contributing/origin/iPhoneOS18.6.sdk/usr/include/expat_config.h

enum macro_expat_config_h {
/*line: 5*/     EXPAT_CONFIG_H = 0x1,  // 1
};

enum macro_byteorder {
/*line: 14*/    BYTEORDER = 0x4d2,  // 1234
};

enum macro_have_arc4random_buf {
/* Define to 1 if you have the `arc4random_buf' function. */
/*line: 21*/    HAVE_ARC4RANDOM_BUF = 0x1,  // 1
};

enum macro_cxx11_support {
/* define if the compiler supports basic C++11 syntax */
/*line: 24*/    HAVE_CXX11 = 0x1,  // 1
};

enum macro_have_dlfcn_h {
/* Define to 1 if you have the <dlfcn.h> header file. */
/*line: 27*/    HAVE_DLFCN_H = 0x1,  // 1
};

enum macro_have_fcntl_h {
/* Define to 1 if you have the <fcntl.h> header file. */
/*line: 30*/    HAVE_FCNTL_H = 0x1,  // 1
};

enum macro_have_getpagesize {
/* Define to 1 if you have the `getpagesize' function. */
/*line: 33*/    HAVE_GETPAGESIZE = 0x1,  // 1
};

enum macro_have_inttypes_h {
/* Define to 1 if you have the <inttypes.h> header file. */
/*line: 39*/    HAVE_INTTYPES_H = 0x1,  // 1
};

enum macro_have_mmap {
/* Define to 1 if you have a working `mmap' system call. */
/*line: 45*/    HAVE_MMAP = 0x1,  // 1
};

enum macro_have_stdint_h {
/* Define to 1 if you have the <stdint.h> header file. */
/*line: 48*/    HAVE_STDINT_H = 0x1,  // 1
};

enum macro_have_stdio_h {
/* Define to 1 if you have the <stdio.h> header file. */
/*line: 51*/    HAVE_STDIO_H = 0x1,  // 1
};

enum macro_have_stdlib_h {
/* Define to 1 if you have the <stdlib.h> header file. */
/*line: 54*/    HAVE_STDLIB_H = 0x1,  // 1
};

enum macro_have_strings_h {
/* Define to 1 if you have the <strings.h> header file. */
/*line: 57*/    HAVE_STRINGS_H = 0x1,  // 1
};

enum macro_have_string_h {
/* Define to 1 if you have the <string.h> header file. */
/*line: 60*/    HAVE_STRING_H = 0x1,  // 1
};

enum macro_have_sys_param_h {
/* Define to 1 if you have the <sys/param.h> header file. */
/*line: 66*/    HAVE_SYS_PARAM_H = 0x1,  // 1
};

enum macro_have_sys_stat_h {
/* Define to 1 if you have the <sys/stat.h> header file. */
/*line: 69*/    HAVE_SYS_STAT_H = 0x1,  // 1
};

enum macro_have_sys_types_h {
/* Define to 1 if you have the <sys/types.h> header file. */
/*line: 72*/    HAVE_SYS_TYPES_H = 0x1,  // 1
};

enum macro_have_unistd_h {
/* Define to 1 if you have the <unistd.h> header file. */
/*line: 75*/    HAVE_UNISTD_H = 0x1,  // 1
};

enum macro_stdc_headers {
/* Define to 1 if all of the C90 standard headers exist (not just the ones
   required in a freestanding environment). This macro is provided for
   backward compatibility; new code need not use it. */
/*line: 104*/   STDC_HEADERS = 0x1,  // 1
};

enum macro_xml_context_bytes {
/* Define to specify how much context to retain around the current parse
   point, 0 to disable. */
/*line: 127*/   XML_CONTEXT_BYTES = 0x400,  // 1024
};

enum macro_use_urandom {
/* Define to include code reading entropy from `/dev/urandom'. */
/*line: 130*/   XML_DEV_URANDOM = 0x1,  // 1
};

enum macro_xml_dtd {
/* Define to make parameter entity parsing functionality available. */
/*line: 133*/   XML_DTD = 0x1,  // 1
};

enum macro_xml_ge {
/* Define as 1/0 to enable/disable support for general entities. */
/*line: 136*/   XML_GE = 0x1,  // 1
};

enum macro_xml_namespace {
/* Define to make XML Namespaces functionality available. */
/*line: 139*/   XML_NS = 0x1,  // 1
};

