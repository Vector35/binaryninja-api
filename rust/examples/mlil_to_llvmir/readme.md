# LLVM IR Lifting Example

This is an example project which lifts/translates MLIL to LLVM IR.

The generated *.ll files from the project can be easily recompiled after being lifted through:
`clang file.ll`.