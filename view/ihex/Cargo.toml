[package]
name = "view-ihex"
version = "0.1.0"
authors = ["Rubens Brandao <git@rubens.io>"]
edition = "2021"

[dependencies]
binaryninja = { path = "../../rust" }
ihex = "3.0.0"
log = "*"

[lib]
crate-type = ["cdylib"]

[profile.release]
panic = "abort"
lto = true
debug = 1
